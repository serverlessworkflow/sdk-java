document:
  dsl: '1.0.0'
  namespace: default
  name: do
  version: '1.0.0'
do:
  - compositeExample:
      do:
        - setRed:
            set:
              colors: ${ .colors + ["red"] }
        - setGreen:
            set:
              colors: ${ .colors + ["green"] }
        - setBlue:
            set:
              colors: ${ .colors + ["blue"] }