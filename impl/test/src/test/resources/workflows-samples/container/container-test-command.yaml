document:
  dsl: '1.0.2'
  namespace: test
  name: container-test-command
  version: '0.1.0'
do:
  - runContainer:
      run:
        container:
          image: alpine:3.20
          command: echo Hello World
          name: hello-world
          lifetime:
            cleanup: never
