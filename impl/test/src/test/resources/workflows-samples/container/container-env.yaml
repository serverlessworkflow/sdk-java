document:
  dsl: '1.0.2'
  namespace: test
  name: run-example
  version: '0.1.0'
do:
  - runContainer:
      run:
        container:
          image: alpine:latest
          command: printenv
          name: hello-world-envs
          lifetime:
            cleanup: never
          environment:
            FOO: ${ .someValue }
            BAR: FOO
