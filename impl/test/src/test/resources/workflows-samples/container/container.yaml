document:
  dsl: '1.0.2'
  namespace: test
  name: run-example
  version: '0.1.0'
do:
  - runContainer:
      run:
        container:
          image: alpine:latest
          #command: echo Hello World
          #command: printenv
          #command: sleep 30
          command: "ls -la /treblereel"
          name: hello-world
          ports:
            8880: 8880
            8881: 8881
            8882: 8882
          lifetime:
            #cleanup: never
            cleanup: never
            #cleanup: eventually
            #after:
            #  seconds: 100
          environment:
            FOO: BAR
            BAR: FOO
          volumes:
            "/Users/treblereel/temp": "/treblereel"
