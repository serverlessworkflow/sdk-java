document:
  dsl: '1.0.2'
  namespace: test
  name: container-timeout
  version: '0.1.0'
do:
  - runContainer:
      run:
        container:
          image: alpine:latest
          command: sleep 300
          name: hello-world-timeout
          lifetime:
            cleanup: eventually
            after:
              seconds: 1