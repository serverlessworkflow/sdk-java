document:
  dsl: 1.0.0-alpha1
  namespace: acme
  name: emit-defaults
  version: 2.1.0
do:
  - emitEvent:
      emit:
        event:
          with:
            type: com.acme.order.placed.v1
            data:
              greetings: ${ "Hello \(.user.firstName)!" }
