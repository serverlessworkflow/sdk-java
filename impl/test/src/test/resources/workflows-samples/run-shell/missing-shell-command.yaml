document:
  dsl: '1.0.1'
  namespace: test
  name: run-shell-example
  version: '0.1.0'
do:
  - missingShellCommand:
      run:
        shell:
          command: ''
          environment:
            FIRST_NAME: John
            LAST_NAME: ${.lastName}

