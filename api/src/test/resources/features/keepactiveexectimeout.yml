id: keepactiveexectimeout
name: Keep Active and Exec Timeout Test Workflow
version: '1.0'
specVersion: '0.7'
timeouts:
  workflowExecTimeout:
    duration: PT1H
    runBefore: GenerateReport
keepActive: true
states: []
